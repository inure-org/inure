#!/usr/bin/env rake

# adicione os seus próprios testes em arquivos localizados em lib/tasks finalizando com .rake,
# por exemplo lib/tasks/capistrano.rake, e eles irão automaticamente estar disponíveis para o Rake.

require File.expand_path('../config/application', __FILE__)

Inure::Application.load_tasks
